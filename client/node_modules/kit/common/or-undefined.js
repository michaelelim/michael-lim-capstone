'use strict';

function orUndefined(...vals) {
  var value = null;

  vals.forEach(function (val) {
    if (value || val === undefined) return;
    value = val;
  });

  return value;
};

module.exports = orUndefined;
