(function () {

  'use strict';

  Polymer(getPolymerDataByClass({{controller}}.default));

  function getPolymerDataByClass(Class) {
    var item = new Class();

    Object.assign(item, {
      is: '{{tagName}}',
    });

    Object
      .getOwnPropertyNames(Class.prototype)
      .filter(function (name) {
        return !['constructor'].includes(name);
      })
      .forEach(function (name) {
        item[name] = Class.prototype[name];
      });

    return item;
  }
}());
