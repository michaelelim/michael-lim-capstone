'use strict';

const _ = {
  getVersions: require('./get-versions'),
};

const state = {

};

function getUrlVersion () {
  var versions = _.getVersions(),
    q = location.search,
    v = q ? q.replace(/[?=]/mgi, '') : versions[0];
  return v;
}

module.exports = getUrlVersion;
