'use strict';

const _ = {

};

const state = {

};

function setUrlVersion (v) {
  var url = `?${v.split('')[0]}=${v.split('')[1]}`;
  history.pushState({}, null, url);
}

module.exports = setUrlVersion;
