'use strict';

const _ = {
  type: require('../type'),
};

const state = {

};

function cssUnit (val, unit = 'px') {
  if (unit === 'px') {
    if (_.type(val) === 'number')
      return `${val}px`;
  }
  return val;
}

module.exports = cssUnit;
