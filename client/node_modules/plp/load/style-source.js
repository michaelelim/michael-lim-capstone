'use strict';

const _ = {
  create: require('../create'),
};

const state = {

};

function loadStyleSource (code) {
  _.create({
    tag: 'style',
    appendTo: 'head',
    html: code,
  });
}

module.exports = loadStyleSource;
