'use strict';

const _ = {
  toBlobUrl: require('../to-blob-url'),

  import: require('./import'),
};

const state = {

};

function loadImportSource (code) {
  return _.import(_.toBlobUrl(code));
}

module.exports = loadImportSource;
