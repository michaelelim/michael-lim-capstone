'use strict';

const _ = {
  concatArrays: require('./concat-arrays'),
};

function ensureArray(...args) {
  return _.concatArrays(
    args.map(
      arg => Array.isArray(arg) ? arg : [arg]
    )
  );
}

module.exports = ensureArray;
