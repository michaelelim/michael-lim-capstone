'use strict';

function filterElements (elems, selector) {
  return Array
    .from(elems)
    .filter(elem => elem.tagName !== 'html'
      && elem !== document
      && elem !== window
      && elem.matches(selector));
}

module.exports = filterElements;
