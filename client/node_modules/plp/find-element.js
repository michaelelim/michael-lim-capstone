'use strict';

const _ = {
  filterElements: require('./filter-elements'),
};

const state = {

};

function findElement (elems, selector) {
  return _.filterElements(elems, selector)[0];
}

module.exports = findElement;
