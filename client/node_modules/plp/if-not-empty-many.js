'use strict';

const _ = {
  splitArray: require('./split-array'),
  ifNotEmpty: require('./if-not-empty'),
};

function ifNotEmptyMany (...items) {
  return _
    .splitArray(items, 2)
    .map(pair => _.ifNotEmpty(pair[0], pair[1]));
}

module.exports = ifNotEmptyMany;
