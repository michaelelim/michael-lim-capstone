'use strict';

const _ = {

};

const state = {

};

function createDecorator (fn, type = 'method') {
  var args = [];
  return wrapper;
  function wrapper(...ar) {
    if (type === 'class' && ar.length === 1 && typeof ar[0] === 'function')
      return fn(ar[0]);
    else if (ar[2] && ar[2].hasOwnProperty('enumerable'))
      return deco(...ar);
    args = ar;
    return deco;
  }
  function deco(...args) {
    if (type === 'class')
      return fn(args[0], ...args.slice(1));
    else
      return fn(args[0], args[1], args[2], ...args.slice(3));
  }
}

module.exports = createDecorator;
