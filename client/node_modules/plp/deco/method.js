'use strict';

const _ = {
  create: require('./create'),
};

const state = {

};

function createMethodDecorator(fn) {
  var args = [];

  return wrapper;

  function wrapper(...ar) {
    if (ar[2] && ar[2].hasOwnProperty('enumerable')) return deco(...ar);
    args = ar;
    return deco;
  }

  function deco(proto, name, desc) {
    return fn(proto, name, desc, ...args);
  }
}

module.exports = createMethodDecorator;
