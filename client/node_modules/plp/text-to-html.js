'use strict';

function textToHTML (text) {
  return text
    .split('\n')
    .map(str => str.trim())
    .filter(str => !!str)
    .map(str => `<p>${str}</p>`)
    .join('');
}

module.exports = textToHTML;
