'use strict';

function splitArray (items, step = 1) {
  var result = [];
  for (let i = 0; i < items.length; i += step) {
    result.push(items.slice(i, i + step));
  }
  return result;
}

module.exports = splitArray;
