'use strict';

const _ = {

};

const state = {

};

function sel (selector, parent = document) {
  return selector instanceof Node
    ? selector
    : document.querySelector(selector);
}

module.exports = sel;
