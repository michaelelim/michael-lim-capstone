function appendImportLink(url) {
  var elem = document.createElement('link');
  elem.setAttribute('rel', 'import');
  elem.setAttribute('href', url);
  document.head.appendChild(elem);
}

export default appendImportLink;
