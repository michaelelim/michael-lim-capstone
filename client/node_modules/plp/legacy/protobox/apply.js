export default apply;

function apply(Class) {

  var props = {};
  Object.getOwnPropertyNames(Class.prototype).forEach(name => {
    props[name] = Class.prototype[name];
  });

  Object.assign(Element.prototype, props);
}
