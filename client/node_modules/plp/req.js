const _ = {
  each: require('./each'),
};

function req(mod) {
  return mod.default || mod;
}

req.hash = function (data) {
  _.each(data, (val, name) => {
    data[name] = req(val);
  });
  return data;
};

req.all = req.hash;

module.exports = req;
