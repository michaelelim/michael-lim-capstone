'use strict';

const _ = {
  extend: require('../extend'),
  
  choose: require('./choose'),
  http: require('./http'),
  files: require('./files'),
};

const state = {

};

function io () {
  
}

_.extend(io, {
  choose: _.choose,
  http: _.http,
  files: _.files,
});

module.exports = io;
