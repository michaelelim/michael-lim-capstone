'use strict';

const _ = {
  isEqual: require('./is-equal'),
};

function topProto (target) {
  var p = target;
  while (p = p.__proto__) {
    if (p.__proto__ === null) return p;
  }
}

module.exports = topProto;
