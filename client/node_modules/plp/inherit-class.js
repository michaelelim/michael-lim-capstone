const _ = {

};

const state = {

};

function inheritClass(Child, Parent) {
  Object.setPrototypeOf(Child.prototype, Object.create(Parent.prototype));
  Object.setPrototypeOf(Child, Parent)
  Child.prototype.constructor = Child;
}

module.exports = inheritClass;
