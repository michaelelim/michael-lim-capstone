'use strict';

const _ = {
  repeat: require('./repeat'),
};

function repeatString(str, count = 1) {
  var result = '';
  _.repeat(count, () => {
    result += str;
  });
  return result;
}

module.exports = repeatString;
