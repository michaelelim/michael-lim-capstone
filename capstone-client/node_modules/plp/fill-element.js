'use strict';

const _ = {

};

const state = {

};

function fillElement (p = null, child = null) {
  var parent = typeof p === 'string'
      ? document.querySelector(p)
      : p;
  if (!parent || !child) return;
  parent.innerHTML = '';
  parent.appendChild(child);
}

module.exports = fillElement;
