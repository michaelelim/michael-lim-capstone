'use strict';

const _ = {
  repeatString: require('./repeat-string'),
};

function indent(str = 1, count = 1, s = '  ') {
  return str
    .split('\n')
    .map(line => `${_.repeatString(s, count)}${line}`)
    .join('\n');
}

module.exports = indent;
