'use strict';

const BackgroundManager = require('./manager.class');

const _ = {

};

const state = {

};

function createBackgroundManager (e) {
  var elem = typeof e === 'string'
    ? document.querySelector(e) : e;
  return new BackgroundManager(elem);
}

module.exports = createBackgroundManager;
