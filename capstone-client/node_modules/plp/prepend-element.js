'use strict';

const _ = {

};

const state = {

};

function prependElement (p = null, child = null) {
  var parent = typeof p === 'string'
      ? document.querySelector(p)
      : p;
  if (!parent || !child) return;
  parent.prepend(child);
}

module.exports = prependElement;
