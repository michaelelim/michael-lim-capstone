'use strict';

const _ = {
  pick: require('../pick'),
};

const state = {

};

function extractCssOptions (data = {}) {
  var predicate = (val, name) => {
      return name.startsWith('$$') && name[2] !== '$';
    },
    bindMethods = (val, name) => {
      return [name.slice(2), val];
    },
    opts = _.pick(data, predicate, bindMethods);
  return opts;
}

module.exports = extractCssOptions;
