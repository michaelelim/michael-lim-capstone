'use strict';

const _ = {

};

const state = {

};

function restoreStyle (elem, name, defaultValue = 'inherit') {
  elem.style[name] = elem.dataset[`_original_${name}`] || defaultValue;
  return elem;
}

module.exports = restoreStyle;
