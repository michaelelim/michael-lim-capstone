'use strict';

const format = require('./format');

function formatCssValue (name, val) {
  var css = format.css({
    [name]: val,
  });
  return css;
}

module.exports = formatCssValue;
