'use strict';

function dasherize (s) {
  var str = s[0].toLowerCase() + s.slice(1);
  return str.replace(/([A-Z])/g, (txt, s) => {
    return `-${s.toLowerCase()}`;
  });
}

module.exports = dasherize;
