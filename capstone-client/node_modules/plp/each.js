'use strict';

/**
function each(items, cb)

*/

const _ = {
  keys: require('./keys'),
};

function each (items, cb = () => {}) {
  if (Array.isArray(items))
    return items.forEach(cb);
  return each(
    _.keys(items),
    key => cb(items[key], key)
  );
}

module.exports = each;
