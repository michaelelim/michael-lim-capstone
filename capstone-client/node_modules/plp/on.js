'use strict';

const _ = {

};

const state = {

};

function on (elem, name = '', cb = function () {}, capture = false) {
  elem.addEventListener(name, cb, capture);
}

module.exports = on;
