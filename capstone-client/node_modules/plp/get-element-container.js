import req from './req';

const _ = {
  isCustom: req(require('./is-custom')),
};

function getElementContainer(elem) {
  return _.isCustom(elem) ? elem.shadowRoot : elem;
}

export default getElementContainer;
