class Bg {

  randomPattern (w, h) {
    var self = this;
    if (w && typeof w === 'object') {
      return Trianglify(w);
    }
    return Trianglify({
      width: w || 300,
      height: h || 300,
      cell_size: _.sample(_.range(40, 170, 10))
    });
  }

}

export default new Bg();
