function onLoaded (handler) {
  if (document.readyState === 'complete' || document.readyState === 'loaded') {
    return handler();
  }
  document.addEventListener('DOMContentLoaded', ev => {
    handler();
  }, false);
}

export default onLoaded;
