import parentNodes from 'util/domtools/parent-nodes';

function matchTree(elem, selector) {
  var parents = parentNodes(elem, true);
  return !!parents
    .filter(elem => elem instanceof Element)
    .filter(parent => parent.matches(selector)).length;
}

export default matchTree;
