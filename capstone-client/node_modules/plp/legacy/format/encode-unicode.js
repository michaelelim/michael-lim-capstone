function encodeUnicode (text, withoutU) {
  return text
    .split('')
    .map(
      s => `\\0${withoutU ? '' : 'u'}${s.charCodeAt(0).toString(16).toUpperCase()}`
    )
    .join('')
}

export default encodeUnicode;
