function clearTags(html, clearExcessSpaces) {
  var val = html.replace(/<\/?[^>]+(>|$)/g, '');
  if (clearExcessSpaces) {
    val = val.replace(/\s+/mgi, ' ');
  }
  return val.trim();
}

export default clearTags;
