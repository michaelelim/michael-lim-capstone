import expression from './expression';

function interpolate(source, ctx) {
  return source.replace(/\[\[([^\]]+)\]\]/mgi, (txt, expr) => {
    return expression(expr, ctx);
  });
}

export default interpolate;
