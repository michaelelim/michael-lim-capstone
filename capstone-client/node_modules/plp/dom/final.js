import QueryWrapper from './query';
import _ from './_';

class FinalWrapper extends QueryWrapper {

  animate (...args) {
    var self = this;
    return self.style.animate(...args);
  }

}

export default FinalWrapper;
