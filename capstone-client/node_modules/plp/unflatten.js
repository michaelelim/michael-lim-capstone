'use strict';

const _ = {
  set: require('./set'),
};

function unflatten (obj) {
  var data = {};
  Object.keys(obj).forEach((name) => {
    _.set(data, name, obj[name]);
  });
  return data;
}

module.exports = unflatten;
