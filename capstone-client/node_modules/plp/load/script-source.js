'use strict';

const _ = {
  toBlobUrl: require('../to-blob-url'),

  script: require('./script'),
};

const state = {

};

function loadScriptSource (code) {
  return _.script(_.toBlobUrl(code));
}

module.exports = loadScriptSource;
