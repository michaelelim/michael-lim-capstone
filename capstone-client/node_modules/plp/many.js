'use strict';

const _ = {

};

const state = {

};

function many (sel, p) {
  var parent = p || document.body;
  return Array.from(parent.querySelectorAll(sel));
}

module.exports = many;
