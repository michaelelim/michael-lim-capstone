'use strict';

const _ = {
  each: require('./each'),
  extend: require('./extend'),
  EventEmitter: require('./event-emitter'),
  aliases: require('./aliases'),
  defaults: require('./defaults'),
  pargs: require('./parse-arguments'),
  is: require('./is'),
  type: require('./type'),
  regFromString: require('./reg-from-string'),
};

import RegularExpression from './re';

function RegularFactory (...args) {
  var self = new RegularExpression(...args);
  return self;
}

$.extend(RegularFactory, {
  fromString: $.regFromString,
  Regular,
  RegularExpression,
});

module.exports = RegularFactory;
