'use strict';

function random (start = 0, finish = 1, precision = 3) {
  if (arguments.length === 1) {
    finish = start;
    start = 0;
  }
  var rand = Math.random(),
    val = (start + ((finish - start) * rand));
  return +val.toFixed(precision);
}

module.exports = random;
