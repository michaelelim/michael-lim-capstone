'use strict';

const _ = require('lodash');
const Database = require('./database');

require('../proto');

module.exports = {

  Database: Database,

  load (name, dir) {
    var self = this,
      db = new Database(name);
    db.loadFromDir(dir);
    return db;
  },

};
