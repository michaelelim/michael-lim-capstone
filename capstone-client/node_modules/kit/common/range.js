'use strict';

function range(start = 0, finish = 100, step = 1) {
  if (arguments.length === 1) {
    finish = start;
    start = 0;
  }
  var items = [];
  for (let i = start; i <= finish; i += step) {
    items.push(i);
  }
  return items;
}

module.exports = range;
