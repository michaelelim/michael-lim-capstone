/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};

/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {

/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;

/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			exports: {},
/******/ 			id: moduleId,
/******/ 			loaded: false
/******/ 		};

/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);

/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;

/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}


/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;

/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;

/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";

/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	__webpack_require__(1);

	var _index = __webpack_require__(23);

	var _index2 = _interopRequireDefault(_index);

	var _common = __webpack_require__(24);

	var _common2 = _interopRequireDefault(_common);

	var _net = __webpack_require__(44);

	var _net2 = _interopRequireDefault(_net);

	var _socket = __webpack_require__(45);

	var _socket2 = _interopRequireDefault(_socket);

	var _events = __webpack_require__(47);

	var _events2 = _interopRequireDefault(_events);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	exports.default = Object.assign(_index2.default, _common2.default, {
	  net: _net2.default, socket: _socket2.default, events: _events2.default,
	  shareAll: shareAll
	});


	function shareAll() {
	  window.doc = window.document;
	  window.body = window.doc.body;
	  window.kit = _index2.default;
	}

/***/ },
/* 1 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	__webpack_require__(2);

	__webpack_require__(4);

	__webpack_require__(5);

	__webpack_require__(6);

	__webpack_require__(7);

	__webpack_require__(8);

	__webpack_require__(9);

	__webpack_require__(10);

	__webpack_require__(12);

	__webpack_require__(13);

	__webpack_require__(14);

	__webpack_require__(15);

	__webpack_require__(16);

	__webpack_require__(17);

	__webpack_require__(18);

	__webpack_require__(19);

	__webpack_require__(20);

/***/ },
/* 2 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _tags = __webpack_require__(3);

	var _tags2 = _interopRequireDefault(_tags);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	var state = {
	  tags: {}
	};

	Object.assign(document, {
	  create: function create() {
	    var tagName = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 'div';
	    var className = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '';
	    var html = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : '';

	    var elem = document.createElement(tagName),
	        classes = elem.parseClassList(className);
	    elem.innerHTML = html || '';
	    elem.data._classes = classes;
	    return elem;
	  },
	  registerTagName: function registerTagName(name, tagName, isContainer) {
	    state.tags[name] = {
	      isContainer: isContainer,
	      tagName: tagName
	    };
	    Element.prototype[name] = function (html, className) {
	      return this.create(tagName, className, html);
	    };
	  },
	  isContainer: function isContainer(tagName) {
	    return !!state.tags[tagName].isContainer;
	  },
	  registerStandardTags: function registerStandardTags() {
	    Object.keys(_tags2.default).forEach(function (tagName) {
	      document.registerTagName(tagName, tagName, _tags2.default[tagName]);
	    });
	  },
	  getRegisteredTags: function getRegisteredTags() {
	    return state.tags;
	  }
	});

	document.registerStandardTags();

	(document._tags || []).forEach(function (item) {
	  document.registerTagName(item.name, item.tagName, item.isContainer);
	});

/***/ },
/* 3 */
/***/ function(module, exports) {

	"use strict";

	var tags = {
	  a: true,
	  abbr: true,
	  address: true,
	  area: true,
	  article: true,
	  aside: true,
	  audio: true,
	  b: false,
	  base: false,
	  bdi: false,
	  bdo: false,
	  blockquote: true,
	  body: true,
	  br: false,
	  button: true,
	  canvas: true,
	  caption: true,
	  cite: true,
	  code: false,
	  col: true,
	  colgroup: true,
	  datalist: false,
	  dd: false,
	  del: false,
	  details: true,
	  dfn: false,
	  dialog: true,
	  div: true,
	  dl: false,
	  dt: false,
	  em: false,
	  embed: true,
	  fieldset: true,
	  figcaption: true,
	  figure: true,
	  footer: true,
	  form: true,
	  h1: true,
	  h2: true,
	  h3: true,
	  h4: true,
	  h5: true,
	  h6: true,
	  head: true,
	  header: true,
	  hr: false,
	  i: false,
	  iframe: false,
	  img: false,
	  input: false,
	  ins: false,
	  kbd: false,
	  keygen: false,
	  label: false,
	  legend: true,
	  li: true,
	  link: false,
	  main: true,
	  map: true,
	  mark: true,
	  menu: true,
	  menuitem: true,
	  meta: false,
	  meter: false,
	  nav: true,
	  noscript: false,
	  object: false,
	  ol: true,
	  optgroup: false,
	  option: true,
	  output: true,
	  p: true,
	  param: false,
	  pre: true,
	  progress: false,
	  q: false,
	  rp: false,
	  rt: false,
	  ruby: false,
	  s: false,
	  samp: false,
	  script: false,
	  section: true,
	  select: false,
	  small: false,
	  source: true,
	  span: false,
	  strong: false,
	  style: true,
	  sub: false,
	  summary: true,
	  sup: false,
	  table: true,
	  tbody: true,
	  td: true,
	  textarea: false,
	  tfoot: true,
	  th: true,
	  thead: true,
	  time: false,
	  title: true,
	  tr: false,
	  track: false,
	  u: true,
	  ul: true,
	  video: true,
	  wbr: false,
	  svg: true
	};

	module.exports = tags;

/***/ },
/* 4 */
/***/ function(module, exports) {

	'use strict';

	Object.assign(Element.prototype, {
	  log: function log() {
	    var self = this;
	    console.log(self);
	    window.elem = self;
	    return self;
	  }
	});

/***/ },
/* 5 */
/***/ function(module, exports) {

	'use strict';

	// Attr Methods ================================================================

	var Classes = [{
	  proto: HTMLTableElement,
	  attrs: ['border', 'cellpadding', 'cellspacing']
	}, {
	  proto: HTMLInputElement,
	  attrs: ['type', 'name', 'placeholder']
	}, {
	  proto: HTMLButtonElement,
	  attrs: ['type', 'name', 'placeholder']
	}, {
	  proto: HTMLImageElement,
	  attrs: ['src', 'alt']
	}, {
	  proto: HTMLAnchorElement,
	  attrs: ['href', 'target']
	}, {
	  proto: HTMLTableCellElement,
	  attrs: ['colspan', 'rowspan']
	}];

	Classes.forEach(function (item) {
	  item.attrs.forEach(function (attr) {
	    Object.defineProperty(item.proto.prototype, attr, {
	      get: function get() {
	        var self = this;
	        return function (val) {
	          self.setAttribute(attr, val);
	          return self;
	        };
	      },
	      set: function set(val) {
	        var self = this;
	        self.setAttribute(attr, val);
	        return self;
	      }
	    });
	  });
	});

/***/ },
/* 6 */
/***/ function(module, exports) {

	'use strict';

	var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

	var state = {
	  counter: 0,
	  data: {}
	};

	Object.assign(Element.prototype, {
	  ds: function ds(name, value) {
	    var self = this;
	    if (arguments.length === 2) {
	      self.dataset[name] = value;
	      return self;
	    }
	    if (arguments.length === 1 && (typeof name === 'undefined' ? 'undefined' : _typeof(name)) === 'object') {
	      _.each(name, function (value, name) {
	        return self.ds(name, value);
	      });
	      return self;
	    }
	    if (arguments.length === 1) {
	      return self.dataset[name];
	    }
	    var data = {};
	    Object.keys(self.dataset).forEach(function (name) {
	      return data[name] = self.dataset[name];
	    });
	    return data;
	  }
	});

	Object.defineProperty(Element.prototype, 'uid', {
	  get: function get() {
	    var self = this,
	        uid = self.dataset.uid || 'uid-' + ++state.counter;
	    self.dataset.uid = uid;
	    return uid;
	  }
	});

	Object.defineProperty(Element.prototype, 'data', {
	  get: function get() {
	    var self = this,
	        uid = self.uid;
	    state.data[uid] = state.data[uid] || {};
	    return state.data[uid];
	  },
	  set: function set(data) {
	    var self = this,
	        uid = self.uid;
	    state.data[uid] = data;
	    return self;
	  }
	});

/***/ },
/* 7 */
/***/ function(module, exports) {

	"use strict";

	Object.assign(Element.prototype, {});

/***/ },
/* 8 */
/***/ function(module, exports) {

	'use strict';

	var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

	Object.assign(Element.prototype, {
	  attr: function attr(name, value) {
	    var self = this;
	    if (arguments.length === 2) {
	      self.setAttribute(name, value);
	      return self;
	    }
	    if (arguments.length === 1 && (typeof name === 'undefined' ? 'undefined' : _typeof(name)) === 'object') {
	      Object.keys(name).forEach(function (key) {
	        self.attr(key, name[key]);
	      });
	      return self;
	    }
	    if (arguments.length === 1) {
	      return self.getAttribute(name);
	    }

	    var attrs = {};
	    Array.from(self.attributes).forEach(function (attr) {
	      return attrs[attr.name] = attr.value;
	    });
	    return attrs;
	  },
	  removeAttr: function removeAttr(name) {
	    var self = this;
	    self.removeAttribute(name);
	  }
	});

/***/ },
/* 9 */
/***/ function(module, exports) {

	'use strict';

	Object.assign(Element.prototype, {
	  html: function html(content) {
	    var self = this;
	    if (arguments.length === 0) {
	      return self.innerHTML;
	    }
	    self.innerHTML = content;
	    return self;
	  },
	  content: function content() {
	    var self = this;
	    return self.html.apply(self, arguments);
	  }
	});

	Object.defineProperty(Element.prototype, '$', {
	  get: function get() {
	    var self = this;
	    return function (val) {
	      self.html(val);
	      return self;
	    };
	  },
	  set: function set(val) {
	    var self = this;
	    self.html(val);
	    return self;
	  }
	});

/***/ },
/* 10 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _tags = __webpack_require__(3);

	var _tags2 = _interopRequireDefault(_tags);

	var _clearTags = __webpack_require__(11);

	var _clearTags2 = _interopRequireDefault(_clearTags);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	Object.assign(Element.prototype, {
	  getLevel: function getLevel() {
	    var self = this,
	        level = self.data._level;
	    if (!level) {
	      level = self.data._level = 0;
	    }
	    return level;
	  },
	  create: function create() {
	    var tagName = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 'div';
	    var className = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '';
	    var content = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : '';

	    var self = this,
	        elem = document.createElement(tagName);
	    self.appendChild(elem);
	    return elem;
	  }
	});

/***/ },
/* 11 */
/***/ function(module, exports) {

	'use strict';

	function clearTags(html) {
	  var clearExcessSpaces = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;

	  var val = html.replace(/<\/?[^>]+(>|$)/g, '');
	  if (clearExcessSpaces) {
	    val = val.replace(/\s+/mgi, ' ');
	  }
	  return val.trim();
	}

	module.exports = clearTags;

/***/ },
/* 12 */
/***/ function(module, exports) {

	'use strict';

	Object.assign(Element.prototype, {});

/***/ },
/* 13 */
/***/ function(module, exports) {

	'use strict';

	Object.assign(Element.prototype, {});

/***/ },
/* 14 */
/***/ function(module, exports) {

	'use strict';

	Object.assign(Element.prototype, {
	  enable: function enable(name) {
	    var self = this;
	    self.classList.add('--' + name);
	    return self;
	  },
	  disable: function disable(name) {
	    var self = this;
	    self.classList.remove('--' + name);
	    return self;
	  },
	  is: function is(name) {
	    var self = this;
	    return self.classList.contains('--' + name);
	  },
	  toggle: function toggle(name) {
	    var self = this;
	    self.is(name) ? self.disable(name) : self.enable(name);
	    return self;
	  }
	});

/***/ },
/* 15 */
/***/ function(module, exports) {

	'use strict';

	Object.assign(Element.prototype, {
	  val: function val(value) {
	    var self = this;
	    if (arguments.length === 1) {
	      if (self.tagName === 'TEXTAREA') {
	        self.html(value);
	        return self;
	      }
	      if (self.type === 'checkbox') {
	        self.checked = !!value;
	        return;
	      }
	      self.value = value;
	      return self;
	    }
	    if (self.tagName === 'TEXTAREA') {
	      return self.html();
	    }
	    if (self.type === 'checkbox') {
	      return !!self.checked;
	    }
	    return self.value;
	  },
	  formData: function formData(data) {
	    var self = this,
	        elem;
	    if (arguments.length === 1) {
	      self.many('[name]').forEach(function (elem) {
	        var name = elem.attr('name');
	        if (!data.hasOwnProperty(name)) return;
	        var value = data[name];
	        if (elem.type === 'radio') {
	          if (elem.val() === value) elem.click();
	          return;
	        }
	        elem.val(value);
	      });
	      return;
	    }
	    data = {};
	    self.many('[name]').forEach(function (elem) {
	      var name = elem.attr('name');
	      if (!name) return;
	      if (elem.type === 'radio') {
	        if (elem.checked) data[name] = elem.val();
	        return;
	      }
	      data[name] = elem.val();
	    });
	    return data;
	  }
	});

/***/ },
/* 16 */
/***/ function(module, exports) {

	'use strict';

	Object.assign(Element.prototype, {});

/***/ },
/* 17 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _tags = __webpack_require__(3);

	var _tags2 = _interopRequireDefault(_tags);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	Object.assign(Node.prototype, {
	  one: function one(selector) {
	    var self = this;
	    return self.querySelector(selector);
	  },
	  many: function many(selector) {
	    var self = this;
	    return Array.from(self.querySelectorAll(selector));
	  }
	});

	// tags.forEach(tag => {
	//   var name, title;
	//   if (typeof tag === 'string') {
	//     name = title = tag;
	//   } else {
	//     name = tag[1];
	//     title = tag[0];
	//   }
	//   if (Node.prototype[title]) return;
	//   Node.prototype[title] = function (html, val) {
	//     var self = this;
	//     return self.create(name, val, html);
	//   };
	// });

/***/ },
/* 18 */
/***/ function(module, exports) {

	'use strict';

	Object.assign(Element.prototype, {
	  toBody: function toBody() {
	    var self = this;
	    return self.appendTo(document.body);
	  },
	  beforeTo: function beforeTo(sibling) {
	    var self = this;
	    sibling.before(self);
	    return self;
	  },
	  before: function before(child) {
	    var self = this,
	        parent = self.parent;
	    if (!parent || !child) return self;
	    parent.insertBefore(child, self);
	    return self;
	  },
	  afterTo: function afterTo(sibling) {
	    var self = this;
	    sibling.after(self);
	    return self;
	  },
	  after: function after(child) {
	    var self = this,
	        parent = self.parent,
	        next = self.nextSibling;
	    if (!parent || !child) return self;
	    if (!next) {
	      parent.appendChild(child);
	      return self;
	    }
	    parent.insertBefore(child, next);
	    return self;
	  },
	  append: function append(child) {
	    var self = this,
	        cl = child.data._classes;
	    if (cl && cl.length) {
	      cl.forEach(function (val) {
	        if (val.startsWith('__')) {
	          val = '' + self.classList[0] + val;
	        }
	        child.classList.add(val);
	      });
	    }
	    self.appendChild(child);
	    return self;
	  },
	  appendTo: function appendTo(parent) {
	    var self = this;
	    parent.appendChild(self);
	    return self;
	  },
	  saveTo: function saveTo(obj, name) {
	    var self = this;
	    obj.data[name] = self;
	    return self;
	  },
	  getParents: function getParents(includeSelf) {
	    var self = this,
	        parents = includeSelf ? [self] : [],
	        parent = self.parent;
	    while (parent && parent !== document) {
	      parents.push(parent);
	      parent = parent.parent;
	    }
	    return parents.filter(function (parent) {
	      return parent instanceof Element;
	    });
	  },
	  findParent: function findParent(selector) {
	    var self = this,
	        parents = self.getParents(true),
	        parent = null;
	    parents.forEach(function (item) {
	      if (parent || !item.matches(selector)) return;
	      parent = item;
	    });
	    return parent;
	  },
	  changeLevel: function changeLevel(val) {
	    var self = this;
	    self.data.level = self.data.level || 0;
	    self.data.level = self.data.level + val;
	    return self;
	  },
	  back: function back(sel) {
	    var self = this;
	    if (typeof sel === 'number') {
	      for (var i = 0; i < sel; i++) {
	        self.parent.data.level = self.data.level;
	        self = self.parent;
	      };
	      self.changeLevel(-1 * sel);
	      return self;
	    }
	    if (sel) return self.findParent(sel);
	    self.changeLevel(-1);
	    return self.parent || self;
	  },
	  up: function up(sel) {
	    return this.back(sel);
	  },
	  to: function to(sel) {
	    return this.back(sel);
	  },
	  out: function out(sel) {
	    return this.back(sel);
	  },
	  top: function top() {
	    var self = this;
	    return self.back('body');
	  }
	});

	Object.defineProperty(Element.prototype, 'parent', {
	  get: function get() {
	    var self = this;
	    return self.parentElement || self.parentNode;
	  }
	});

	// ['_', '__', '___', '____', '_____',
	// '______', '_______', '________'].forEach(sign => {
	//   Object.defineProperty(Element.prototype, sign, {
	//     get () {
	//       var self = this;
	//       for (let i = 0; i < sign.length; i++) {
	//         self = self.back();
	//       }
	//       return self;
	//     },
	//   });
	// });

/***/ },
/* 19 */
/***/ function(module, exports) {

	'use strict';

	Object.assign(Element.prototype, {
	  css: function css(name, value) {
	    var self = this;
	    if (arguments.length === 2) {
	      self.style[name] = value;
	      return self;
	    }
	    if (arguments.length === 1 && typeof name === 'string') {
	      return window.getComputedStyle(self, null).getPropertyValue(name);
	    }
	    if (arguments.length === 1) {
	      Object.keys(name).forEach(function (key) {
	        return self.css(key, name[key]);
	      });
	      return self;
	    }
	    var data = {},
	        styles = window.getComputedStyle(self, null);
	    Array.from(styles).forEach(function (name) {
	      return data[name] = styles.getPropertyValue(name);
	    });
	    return data;
	  }
	});

/***/ },
/* 20 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _flatten = __webpack_require__(21);

	var _flatten2 = _interopRequireDefault(_flatten);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	Object.assign(Element.prototype, {
	  parseClassList: function parseClassList() {
	    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
	      args[_key] = arguments[_key];
	    }

	    var self = this,
	        classes = (0, _flatten2.default)(args).filter(function (item) {
	      return item && typeof item === 'string';
	    }).map(function (item) {
	      return item.split(/[\.\s]/);
	    });
	    return (0, _flatten2.default)(classes).filter(function (item) {
	      return item && typeof item === 'string';
	    });
	  },
	  addClass: function addClass() {
	    var self = this,
	        parent = self.parentNode;

	    for (var _len2 = arguments.length, args = Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
	      args[_key2] = arguments[_key2];
	    }

	    self.parseClassList(args).forEach(function (item) {
	      if (!parent || !item.startsWith('__') || !parent.classList.length) {
	        return self.classList.add(item);
	      }
	      self.classList.add(parent.classList[0] + '__' + item);
	    });
	    return self;
	  },
	  removeClass: function removeClass() {
	    var self = this;

	    for (var _len3 = arguments.length, args = Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
	      args[_key3] = arguments[_key3];
	    }

	    self.parseClassList(args).forEach(function (val) {
	      return self.classList.remove(val);
	    });
	    return self;
	  },
	  toggleClass: function toggleClass() {
	    var self = this;

	    for (var _len4 = arguments.length, args = Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
	      args[_key4] = arguments[_key4];
	    }

	    self.parseClassList(args).forEach(function (val) {
	      return self.classList.toggle(val);
	    });
	    return self;
	  },
	  hasClass: function hasClass(val) {
	    var self = this;
	    return self.classList.contains(val);
	  },
	  cls: function cls() {
	    var self = this;
	    return self.addClass.apply(self, arguments);
	  }
	});

/***/ },
/* 21 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

	var set = __webpack_require__(22);

	function flatten(obj, parentName) {
	  var data = {};
	  if (Array.isArray(obj)) return flattenArray(obj);
	  parentName = parentName || '';
	  Object.keys(obj).forEach(function (name) {
	    var val = obj[name];
	    name = parentName ? parentName + '.' + name : name;
	    if ((typeof val === 'undefined' ? 'undefined' : _typeof(val)) !== 'object') {
	      data[name] = val;
	      return;
	    }
	    Object.assign(data, flatten(val, name));
	  });
	  return data;
	}

	function flattenArray(arr) {
	  var items = [];
	  Array.from(arr, function (item) {
	    if (!item || (typeof item === 'undefined' ? 'undefined' : _typeof(item)) !== 'object') {
	      return items.push(item);
	    }
	    if (item.length !== undefined) {
	      item = Array.from(item);
	    }
	    items = items.concat(flattenArray(item));
	  });
	  return items;
	}

	flatten.reverse = flatten.undo = flatten.invert = flatten.unflat = function (obj) {
	  var data = {};
	  Object.keys(obj).forEach(function (name) {
	    set(data, name, obj[name]);
	  });
	  return data;
	};

	module.exports = flatten;

/***/ },
/* 22 */
/***/ function(module, exports) {

	'use strict';

	function set(obj, p, value) {
	  var current = obj,
	      names = p.split('.'),
	      name;
	  for (var i = 0; i < names.length - 1; i++) {
	    name = names[i];
	    if (!current[name]) {
	      current[name] = {};
	    }
	    current = current[name];
	  }
	  current[names.pop()] = value;
	  return obj;
	}

	module.exports = set;

/***/ },
/* 23 */
/***/ function(module, exports) {

	'use strict';

	var kit = module.exports = {};

/***/ },
/* 24 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

	module.exports = {
	  clearTags: __webpack_require__(11),
	  datetime: __webpack_require__(25),
	  encodeUnicode: __webpack_require__(26),
	  filesize: __webpack_require__(27),
	  shortenByWords: __webpack_require__(28),
	  textToHtml: __webpack_require__(29),
	  trimFilename: __webpack_require__(30),
	  createFunction: __webpack_require__(31),
	  expression: __webpack_require__(32),
	  flatten: __webpack_require__(21),
	  interpolate: __webpack_require__(33),
	  set: __webpack_require__(22),
	  get: __webpack_require__(34),
	  round: __webpack_require__(35),
	  argsList: __webpack_require__(36),
	  unicode: __webpack_require__(37),
	  promiseFactory: __webpack_require__(38),
	  mapToObject: __webpack_require__(39),
	  objectsToArray: __webpack_require__(40),
	  methodsNames: __webpack_require__(41),
	  injectMethods: __webpack_require__(42),

	  EventEmitter: __webpack_require__(43),

	  resolver: resolver, isObject: isObject, notObject: notObject
	};

	function resolver(dir) {
	  return function (file) {
	    return path.resolve(dir, file);
	  };
	}

	function isObject(item) {
	  return item && (typeof item === 'undefined' ? 'undefined' : _typeof(item)) === 'object' && !Array.isArray(item);
	}

	function notObject(item) {
	  return isObject(item);
	}

/***/ },
/* 25 */
/***/ function(module, exports) {

	'use strict';

	var datetime = {
	  full: full
	};

	function full(d) {
	  return ('\n    ' + z(d.getDate()) + '.' + z(d.getMonth() + 1) + '.' + z(d.getFullYear()) + '\n    ' + z(d.getHours()) + ':' + z(d.getMinutes()) + ':' + z(d.getSeconds()) + '\n  ').trim().replace(/\s+/mgi, ' ');
	}

	function z(val) {
	  val = val + '';
	  return val.length === 1 ? '0' + val : val;
	}

	module.exports = datetime;

/***/ },
/* 26 */
/***/ function(module, exports) {

	'use strict';

	function encodeUnicode(text, withoutU) {
	  return text.split('').map(function (s) {
	    return '\\0' + (withoutU ? '' : 'u') + s.charCodeAt(0).toString(16).toUpperCase();
	  }).join('');
	}

	module.exports = encodeUnicode;

/***/ },
/* 27 */
/***/ function(module, exports) {

	"use strict";

	function filesize(val) {
	  if (val < 1024) {
	    return val + " B";
	  }
	  val = Math.round(val / 1024);
	  if (val < 1024) {
	    return val + " KB";
	  }
	  val = Math.round(val / 1024);
	  if (val < 1024) {
	    return val + " MB";
	  }
	  val = Math.round(val / 1024);
	  if (val < 1024) {
	    return val + " GB";
	  }
	  val = Math.round(val / 1024);
	  if (val < 1024) {
	    return val + " TB";
	  }
	  val = Math.round(val / 1024);
	  if (val < 1024) {
	    return val + " PB";
	  }
	}

	module.exports = filesize;

/***/ },
/* 28 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var clearTags = __webpack_require__(11);

	function shortenByWords(html) {
	  var count = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 7;

	  var val = clearTags(html),
	      words = val.split(' ').map(function (s) {
	    return (s || '').trim();
	  }).filter(function (s) {
	    return !!s;
	  }),
	      cleanWords = words.filter(function (s) {
	    return !['.', '!', ',', '(', ')'].includes(s);
	  });
	  if (cleanWords.length <= count) {
	    return val;
	  }
	  return words.slice(0, count).join(' ') + '...';
	}

	module.exports = shortenByWords;

/***/ },
/* 29 */
/***/ function(module, exports) {

	'use strict';

	function textToHTML(text) {
	  return text.split('\n').map(function (str) {
	    return str.trim();
	  }).filter(function (str) {
	    return !!str;
	  }).map(function (str) {
	    return '<p>' + str + '</p>';
	  }).join('');
	}

	module.exports = textToHTML;

/***/ },
/* 30 */
/***/ function(module, exports) {

	'use strict';

	function trimFilename(filename) {
	  var count = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 10;
	  var trimLastNumbers = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
	  var splitExt = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : false;

	  var parts = filename.split('.'),
	      extname = parts.pop() || '',
	      basename = parts.join('.');
	  if (trimLastNumbers) {
	    basename = basename.replace(/\-(\d+)$/, '');
	  }
	  if (basename.length > count) {
	    basename = basename.slice(0, count) + '.';
	  }
	  if (splitExt) {
	    return [basename, extname];
	  }
	  return basename + '.' + extname;
	}

	module.exports = trimFilename;

/***/ },
/* 31 */
/***/ function(module, exports) {

	'use strict';

	function createFunction(source, ctx, args) {
	  var context = ctx || {},
	      names = Object.keys(context),
	      values = Object.values(context),
	      argsStr = names.concat(args || []).join(','),
	      fn = new Function(argsStr, source);

	  return function () {
	    return fn.apply(context.self || null, values.concat([].slice.call(arguments, 0)));
	  };
	}

	module.exports = createFunction;

/***/ },
/* 32 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var createFunction = __webpack_require__(31);

	function expression(source, ctx) {
	  var fn = createFunction('return ' + source + ';', ctx);
	  return fn();
	}

	module.exports = expression;

/***/ },
/* 33 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var expression = __webpack_require__(32);

	function interpolate(source, ctx) {
	  return source.replace(/\[\[([^\]]+)\]\]/mgi, function (txt, expr) {
	    return expression(expr, ctx);
	  });
	}

	module.exports = interpolate;

/***/ },
/* 34 */
/***/ function(module, exports) {

	'use strict';

	function get(obj, p) {
	  var current = obj,
	      names = p.split('.'),
	      name;
	  for (var i = 0; i < names.length - 1; i++) {
	    name = names[i];
	    if (!current[name]) {
	      current[name] = {};
	    }
	    current = current[name];
	  }
	  return current[names.pop()];
	}

	module.exports = get;

/***/ },
/* 35 */
/***/ function(module, exports) {

	"use strict";

	function roundMin(x, step) {
	  step = step || 1;
	  return Math.ceil(x - x % step);
	}

	function roundMax(x, step) {
	  step = step || 1;
	  return Math.ceil(x - x % step + step);
	}

	module.exports = {
	  min: roundMin,
	  max: roundMax
	};

/***/ },
/* 36 */
/***/ function(module, exports) {

	'use strict';

	function argsList(fn) {
	  var re = /function\s+(?:[^\(]+)?\s*\(([^\)]+)/i,
	      source = fn.toString(),
	      result = re.exec(source) || [],
	      argsStr = result[1],
	      args = argsStr ? argsStr.split(',') : [];

	  return args.map(function (name) {
	    return name.trim();
	  }).filter(function (name) {
	    return !!name;
	  });
	}

	module.exports = argsList;

/***/ },
/* 37 */
/***/ function(module, exports) {

	"use strict";

	function decodeChars(bin) {
	  return String.fromCharCode.apply(null, new Uint8Array(bin));
	}

	function decodeUnicode(arrayBuffer) {
	  var result = "",
	      i = 0,
	      c = 0,
	      c1 = 0,
	      c2 = 0,
	      data = new Uint8Array(arrayBuffer);
	  if (data.length >= 3 && data[0] === 0xef && data[1] === 0xbb && data[2] === 0xbf) {
	    i = 3;
	  }
	  while (i < data.length) {
	    c = data[i];
	    if (c < 128) {
	      result += String.fromCharCode(c);
	      i++;
	    } else if (c > 191 && c < 224) {
	      if (i + 1 >= data.length) {
	        throw "UTF-8 Decode failed. Two byte character was truncated.";
	      }
	      c2 = data[i + 1];
	      result += String.fromCharCode((c & 31) << 6 | c2 & 63);
	      i += 2;
	    } else {
	      if (i + 2 >= data.length) {
	        throw "UTF-8 Decode failed. Multi byte character was truncated.";
	      }
	      c2 = data[i + 1];
	      c3 = data[i + 2];
	      result += String.fromCharCode((c & 15) << 12 | (c2 & 63) << 6 | c3 & 63);
	      i += 3;
	    }
	  }
	  return result;
	}

	module.exports = {
	  decodeChars: decodeChars,
	  decodeUnicode: decodeUnicode
	};

/***/ },
/* 38 */
/***/ function(module, exports) {

	'use strict';

	function parseFunctionArgs(fn) {
	  return fn.toString().split(')')[0].split('(')[1].split(',').map(function (arg) {
	    return arg.trim();
	  });
	}

	function createPromiseFactory(fn, ctx) {
	  return function () {
	    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
	      args[_key] = arguments[_key];
	    }

	    var context = _.extend({}, ctx),
	        argsNames = parseFunctionArgs(fn);
	    return new Promise(function (resolve, reject) {
	      if (argsNames.includes('resolve') || argsNames.includes('res')) {
	        return fn.apply(context, args.concat([resolve, reject]));
	      }
	      try {
	        var result = fn.apply(context, args);
	        if (result instanceof Promise) {
	          return result.then(resolve).catch(reject);
	        }
	        resolve(result);
	      } catch (err) {
	        reject(err);
	      }
	    });
	  };
	}

	module.exports = createPromiseFactory;

/***/ },
/* 39 */
/***/ function(module, exports) {

	"use strict";

	function mapToObject(arr, handler) {
	  var map = {};
	  self.each(arr, function (value, i) {
	    var temp = handler(value, i);
	    if (!temp || !temp.name) return;
	    map[temp.name] = temp.value;
	  });
	  return map;
	}

	module.exports = mapToObject;

/***/ },
/* 40 */
/***/ function(module, exports) {

	'use strict';

	var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

	function objectsToArray(data) {
	  var propName = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '_name';

	  return Object.keys(data).map(function (name) {
	    var obj = data[name];
	    if (!obj || (typeof obj === 'undefined' ? 'undefined' : _typeof(obj)) !== 'object') return;
	    obj[propName] = name;
	    return obj;
	  }).filter(function (item) {
	    return !!item;
	  });
	}

	module.exports = objectsToArray;

/***/ },
/* 41 */
/***/ function(module, exports) {

	'use strict';

	function getClassMethods(Class) {
	  var exclude = ['constructor', 'length'];
	  return Object.getOwnPropertyNames(Class.prototype).filter(function (name) {
	    return !exclude.includes(name);
	  });
	}

	function getStaticMethods(Class) {
	  var exclude = ['length', 'name', 'prototype'];
	  return Object.getOwnPropertyNames(Class).filter(function (name) {
	    return !exclude.includes(name);
	  });
	}

	module.exports = {
	  getClassMethods: getClassMethods,
	  getStaticMethods: getStaticMethods
	};

/***/ },
/* 42 */
/***/ function(module, exports) {

	'use strict';

	function injectMethods(target, source) {
	  _.each(source, function (method, name) {
	    if (typeof method !== 'function') return;
	    target[name] = toMethod(method);
	  });
	  return target;
	}

	function toMethod(fn) {
	  return function () {
	    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
	      args[_key] = arguments[_key];
	    }

	    args.unshift(this);
	    return fn.apply(this, args);
	  };
	}

	injectMethods.toMethod = toMethod;

	module.exports = injectMethods;

/***/ },
/* 43 */
/***/ function(module, exports) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	var EventEmitter = function () {
	  function EventEmitter() {
	    _classCallCheck(this, EventEmitter);

	    var self = this;
	    self._handlers = {};
	  }

	  _createClass(EventEmitter, [{
	    key: "on",
	    value: function on(name, handler, oneTime) {
	      var self = this,
	          handlers = self._handlers;
	      handlers[name] = handlers[name] || [];
	      handlers[name].push({
	        oneTime: !!oneTime,
	        handler: handler
	      });
	      return self;
	    }
	  }, {
	    key: "one",
	    value: function one(name, handler) {
	      var self = this;
	      return self.on(name, handler, true);
	    }
	  }, {
	    key: "emit",
	    value: function emit(name) {
	      var self = this,
	          args = Array.prototype.slice.call(arguments, 1),
	          handlers = self._handlers;
	      _.each(handlers[name], function (ev, i) {
	        ev.handler.apply(self, args);
	        if (ev.oneTime) {
	          handlers[name].splice(i, 1);
	        }
	      });
	      return self;
	    }
	  }]);

	  return EventEmitter;
	}();

	exports.default = EventEmitter;

/***/ },
/* 44 */
/***/ function(module, exports) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	var NetUtil = function () {
	  function NetUtil() {
	    _classCallCheck(this, NetUtil);
	  }

	  _createClass(NetUtil, [{
	    key: 'get',
	    value: function get(url) {
	      var self = this,
	          xhr = new XMLHttpRequest();
	      return new Promise(function (res, rej) {
	        xhr.addEventListener('load', function (ev) {
	          res(xhr.responseText);
	        });
	        xhr.addEventListener('error', function (ev) {
	          rej();
	        });
	        xhr.open('GET', url);
	        xhr.send();
	      });
	    }
	  }, {
	    key: 'getJSON',
	    value: function getJSON(url) {
	      var self = this;
	      return new Promise(function (res, rej) {
	        self.get(url).then(function (content) {
	          res(JSON.parse(content));
	        }).catch(rej);
	      });
	    }
	  }]);

	  return NetUtil;
	}();

	exports.default = NetUtil;

/***/ },
/* 45 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _eventEmitter = __webpack_require__(46);

	var _eventEmitter2 = _interopRequireDefault(_eventEmitter);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var Socket = function (_EventEmitter) {
	  _inherits(Socket, _EventEmitter);

	  function Socket(url) {
	    _classCallCheck(this, Socket);

	    var _this = _possibleConstructorReturn(this, (Socket.__proto__ || Object.getPrototypeOf(Socket)).call(this));

	    var self = _this;
	    self.url = url;
	    self.ws = null;
	    self.waiting = [];
	    self.connected = true;

	    self.init(url);
	    return _this;
	  }

	  _createClass(Socket, [{
	    key: 'init',
	    value: function init(url) {
	      var self = this;
	      if (self.ws) {
	        return;
	      }
	      self.ws = new WebSocket(url);
	      self.ws.onopen = function () {
	        (self.waiting || []).forEach(function (content) {
	          self.ws.send(content);
	        });
	      };
	      self.ws.onmessage = self.onMessage.bind(self);
	    }
	  }, {
	    key: 'onMessage',
	    value: function onMessage(ev) {
	      var self = this,
	          content = ev.data,
	          response = JSON.parse(content);
	      self.emit(response.name, response.data);
	    }
	  }, {
	    key: 'send',
	    value: function send(name, data) {
	      var self = this,
	          request = {
	        name: name,
	        data: data
	      },
	          content = JSON.stringify(request);
	      if (self.ws.readyState !== self.ws.OPEN) {
	        self.waiting.push(content);
	        return;
	      }
	      self.ws.send(content);
	    }
	  }]);

	  return Socket;
	}(_eventEmitter2.default);

	module.exports = function () {
	  function SocketUtil() {
	    _classCallCheck(this, SocketUtil);
	  }

	  _createClass(SocketUtil, [{
	    key: 'init',
	    value: function init() {
	      var self = this;
	    }
	  }, {
	    key: 'socket',
	    value: function socket(url) {
	      return new Socket(url);
	    }
	  }]);

	  return SocketUtil;
	}();

/***/ },
/* 46 */
/***/ function(module, exports) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	var EventEmitter = function () {
	  function EventEmitter() {
	    _classCallCheck(this, EventEmitter);

	    var self = this;
	    self._handlers = {};
	  }

	  _createClass(EventEmitter, [{
	    key: "on",
	    value: function on(name, handler, oneTime) {
	      var self = this,
	          handlers = self._handlers;
	      handlers[name] = handlers[name] || [];
	      handlers[name].push({
	        oneTime: !!oneTime,
	        handler: handler
	      });
	      return self;
	    }
	  }, {
	    key: "one",
	    value: function one(name, handler) {
	      var self = this;
	      return self.on(name, handler, true);
	    }
	  }, {
	    key: "emit",
	    value: function emit(name) {
	      var self = this,
	          args = Array.prototype.slice.call(arguments, 1),
	          handlers = self._handlers;
	      _.each(handlers[name], function (ev, i) {
	        ev.handler.apply(self, args);
	        if (ev.oneTime) {
	          handlers[name].splice(i, 1);
	        }
	      });
	      return self;
	    }
	  }]);

	  return EventEmitter;
	}();

	exports.default = EventEmitter;

/***/ },
/* 47 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _eventEmitter = __webpack_require__(46);

	var _eventEmitter2 = _interopRequireDefault(_eventEmitter);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	module.exports = {
	  EventEmitter: _eventEmitter2.default
	};

/***/ }
/******/ ]);